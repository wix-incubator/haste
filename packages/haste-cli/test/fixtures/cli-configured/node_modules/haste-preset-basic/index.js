module.exports = {
  build: () => {
    console.log('running build...');
    return Promise.resolve();
  },
  test: () => {
    console.log('running test...');
    return Promise.reject()
  },
  persistent: (configure) => {
    console.log('running persistent...');
    configure({ persistent: true });

    return Promise.resolve();
  },
};
